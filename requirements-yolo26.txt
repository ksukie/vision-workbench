--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu126

-r requirements.txt

# Install the local YOLO26/Ultralytics source and its runtime dependencies.
# Run this from the VisionWorkbench project root:
# pip install -r requirements-yolo26.txt
#
# For automatic CUDA/CPU selection, prefer:
# python scripts/install_dependencies.py yolo26
torch==2.12.1+cu126; platform_system == "Windows" and platform_machine == "AMD64"
torchvision==0.27.1+cu126; platform_system == "Windows" and platform_machine == "AMD64"
torch==2.12.1+cu126; platform_system == "Linux" and platform_machine == "x86_64"
torchvision==0.27.1+cu126; platform_system == "Linux" and platform_machine == "x86_64"
torch==2.12.1; platform_system == "Darwin"
torchvision==0.27.1; platform_system == "Darwin"
torch==2.12.1; platform_system == "Windows" and platform_machine != "AMD64"
torchvision==0.27.1; platform_system == "Windows" and platform_machine != "AMD64"
torch==2.12.1; platform_system == "Linux" and platform_machine != "x86_64"
torchvision==0.27.1; platform_system == "Linux" and platform_machine != "x86_64"
-e ./third_party/yolo26_source
