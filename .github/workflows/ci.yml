name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  tests:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.12"]
    runs-on: ${{ matrix.os }}
    env:
      PYTHONNOUSERSITE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install Linux Qt runtime
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes libegl1
      - name: Install locked base dependencies
        run: python -m pip install --require-hashes --requirement requirements-base.lock
      - name: Install pytest
        run: python -m pip install pytest==9.0.3
      - name: Install project
        run: python -m pip install --no-deps --editable .
      - name: Smoke-test Qt shell
        run: python -m vision_workbench.self_test --expected-mode editable --qt
      - name: Compile sources
        run: python -W error -m compileall -q src tests scripts
      - name: Check version contract
        run: python scripts/check_version_contract.py
      - name: Run tests
        run: python -m pytest -q
      - name: Check Markdown links
        run: python scripts/check_markdown_links.py
